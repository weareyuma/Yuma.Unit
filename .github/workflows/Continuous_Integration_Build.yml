# ------------------------------------------------------------------------------
# <auto-generated>
#
#     This code was generated.
#
#     - To turn off auto-generation set:
#
#         [GitHubActions (AutoGenerate = false)]
#
#     - To trigger manual generation invoke:
#
#         nuke --generate-configuration GitHubActions_Continuous_Integration_Build --host GitHubActions
#
# </auto-generated>
# ------------------------------------------------------------------------------

name: Continuous_Integration_Build

on:
  push:
    branches-ignore:
      - main
      - 'feature/*'

jobs:
  ubuntu-latest:
    name: ubuntu-latest
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          fetch-depth: 0
      - name: 'Cache: .nuke/temp, ~/.nuget/packages'
        uses: actions/cache@v4
        with:
          path: |
            .nuke/temp
            ~/.nuget/packages
          key: ${{ runner.os }}-${{ hashFiles('**/global.json', '**/*.csproj', '**/Directory.Packages.props') }}
      - name: 'Run: CI'
        run: ./build.cmd CI
        env:
          YumaReleaseFeedApiKey: ${{ secrets.YUMA_RELEASE_FEED_API_KEY }}
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
      - name: 'Publish: nuget-packages'
        uses: actions/upload-artifact@v4
        with:
          name: nuget-packages
          path: artifacts/nuget-packages
      - name: 'Publish: test-coverage-reports'
        uses: actions/upload-artifact@v4
        with:
          name: test-coverage-reports
          path: artifacts/test-coverage-reports
      - name: 'Publish: mutation-report.html'
        uses: actions/upload-artifact@v4
        with:
          name: mutation-report.html
          path: artifacts/reports/mutation-report.html
